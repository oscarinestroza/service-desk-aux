services:
  db:
    image: postgres:16-alpine
    container_name: enlacesmao_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: enlacesmao
      POSTGRES_USER: enlacesmao
      POSTGRES_PASSWORD: enlacesmao
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U enlacesmao -d enlacesmao"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: enlacesmao_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
  redisdata:
  media: